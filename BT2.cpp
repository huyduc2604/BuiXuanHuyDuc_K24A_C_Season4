#include <stdio.h>

int main (){
	int n; printf("Hay nhap mot so: "); scanf ("%d", &n);
	if (n % 2 ==0){
		printf ("Day la mot so chan");
	} 
	else{
		("Day la mot so le");
	} 
	return 0; 
} 
