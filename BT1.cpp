#include <stdio.h>

int main (){
	int n; printf ("Hay nhap mot so nguyen"); scanf("%d", &n);
	if(n>0){
		printf ("day la mot so duong");
	}else{
		printf ("day la mot so am");
	}
	return 0;
}
